# Systems

Reusable game systems live here.

- `InputController.ts` merges keyboard, touch, and tilt into one player input state.
- `TiltController.ts` handles iOS/browser orientation permission and normalized tilt values.
